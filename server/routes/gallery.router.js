const express = require('express');
const router = express.Router();

let galleryList = require()
// PUT /gallery/like/:id
router.put('/like/:id', (req, res) => {
s({
  method: 'get',

})
});

// GET /gallery
router.get('/api/gallery', (req, res) => {
  // code here
});

module.exports = router;
